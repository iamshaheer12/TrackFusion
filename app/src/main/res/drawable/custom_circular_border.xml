<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" /> <!-- Background color, can be changed -->
        </shape>
    </item>

    <!-- Border -->
    <item android:left="1dp" android:right="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Transparent background for the border -->
            <stroke android:width="2dp" android:color="@android:color/white" /> <!-- Border width and color -->
            <corners android:radius="30dp" /> <!-- Optional: corner radius -->
        </shape>
    </item>

</layer-list>